{
  "name": "sinuous-all",
  "private": true,
  "version": "0.0.1",
  "description": "Sinuous all",
  "module": "../module/all.js",
  "main": "../dist/all.js",
  "files": [
    "src"
  ],
  "scripts": {
    "lint": "eslint src",
    "prepublishOnly": "yarn lint && (cd ../../.. && yarn build)"
  }
}
