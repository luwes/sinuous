[build]
  publish = "site/public"
  command = "yarn build --all && (cd site && yarn build)"

[build.environment]
  NODE_VERSION = "16.17.0"
  YARN_VERSION = "1.22.19"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
