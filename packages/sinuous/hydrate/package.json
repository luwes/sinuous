{
  "name": "sinuous-hydrate",
  "private": true,
  "version": "0.0.1",
  "description": "Sinuous hydrate",
  "module": "../dist/esm/hydrate.js",
  "main": "../dist/umd/hydrate.js",
  "types": "src/index.d.ts",
  "files": [
    "src"
  ],
  "scripts": {
    "lint": "eslint src",
    "prepublishOnly": "yarn lint && (cd ../../.. && yarn build)"
  }
}
